# Domain Configuration
# For development: Leave blank or use ":80"
# For production: Use your domain name (e.g., "zetteln.yourdomain.com")
# Caddy will automatically obtain SSL certificates for production domains
DOMAIN=:80

# Port Configuration
# Development defaults: 8000 (HTTP), 8443 (HTTPS)
# Production defaults: 80 (HTTP), 443 (HTTPS)
# Uncomment to override:
# HOST_HTTP_PORT=8000
# HOST_HTTPS_PORT=8443

# Frontend Development Configuration (optional)
# Custom API URL for local dev server (npm run dev)
# Useful for developing against remote/staging APIs
# Examples:
#   VITE_API_URL=http://localhost:9000
#   VITE_API_URL=https://staging.example.com
#   VITE_API_URL=https://api.production.com
# Default: http://localhost:${HOST_HTTP_PORT:-8000}
# VITE_API_URL=
